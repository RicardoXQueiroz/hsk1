flask>=3.1.1
flask-cors>=4.0.0
gunicorn>=21.2.0

# gtts NÃO precisa estar aqui — ele só é usado no script gerar_audios.py,
# que você roda LOCALMENTE (não no Render) pra gerar os mp3s com antecedência.
